# Design System - TrueCut Barbershop

## Product Context
- **What this is:** A multi-branch barbershop booking and operations platform - client booking, receptionist walk-in/payment handling, barber schedules, and company-admin financial/operational reporting.
- **Who it's for:** Barbershop owners, receptionists, barbers, and clients in Harare, Zimbabwe. Staff users (admin/receptionist/barber) are the primary daily users; clients book occasionally.
- **Space/industry:** Barbershop/grooming booking software, competing conceptually with Fresha, GlossGenius, and Squire - but positioned as an executive, precision-run operation rather than a generic consumer booking app.
- **Project type:** Hybrid - operational web app (dashboards, schedules, reports) plus a lighter marketing/booking front for clients.

## Aesthetic Direction
- **Direction:** Quiet Precision - Industrial-Executive, dialed back down, in a classic barbershop palette. Supersedes "Vibrant Craft" (2026-09-22): the fire-orange/warm-gradient treatment read as generic and AI-generated rather than professional. The accent moved to a deep maroon/burgundy with warm beige-and-near-black neutrals - a traditional barbershop-pole/barber-chair palette - and decoration was pared back to a single static glow instead of multiple pulsing ones.
- **Decoration level:** Restrained. Background glows are static (no `animate-pulse`) and low-opacity (~10%); gradients are used sparingly (hero headline, primary CTA) and stay tonal (maroon-to-maroon) rather than mixing multiple hues. Cards still carry the persistent floating shadow (see Motion) - that part of "Vibrant Craft" stays, since it reads as tactile/polished rather than flashy.
- **Mood:** Precision and trust, expressed with restraint - confident and competent, not "designed to impress." Classic and standard over distinctive; evokes a traditional barbershop rather than a tech startup.
- **Memorable thing:** Maroon, beige, and near-black - a warm but restrained, classic barbershop palette, floating cards, and generous whitespace, not a wash of competing gradient colors.
- **Reference sites (category landscape):** Fresha, GlossGenius, Squire (flat, generic) vs. a car-marketplace reference site (dark navy hero, single-accent label, one restrained gradient CTA, flat professional cards) - taken as a cue toward calm, standard-issue professionalism rather than a color-forward consumer-marketplace look, then re-colored into a maroon/beige barbershop identity rather than the reference's own cool blue tones.

## Typography
- **Display/Hero:** Inter (700/800/900) - the standard, ubiquitous grotesk sans; replaces Satoshi's distinctive rounded warmth with a classic, unremarkable choice that matches the "not designed to look AI-generated" direction. Used for headings, hero copy, section titles. Loaded via Google Fonts: `https://fonts.googleapis.com/css2?family=Inter:wght@700;800;900&display=swap`
- **Body/UI:** General Sans (400/500/600/700) - clean and highly legible for buttons, labels, forms, paragraph text. Loaded via Fontshare: `https://api.fontshare.com/v2/css?f[]=general-sans@400,500,600,700&display=swap`
- **Data/Tables:** Geist (400/500/600/700), `font-variant-numeric: tabular-nums` - for revenue figures, timestamps, and any tabular data where digits must align. Loaded via Google Fonts: `https://fonts.googleapis.com/css2?family=Geist:wght@400;500;600;700&display=swap`
- **Code:** not applicable to this product's UI.
- **Scale:** Hero/H1 44px/900, H2 24px/800, body 14-16px/400-600, data values 26-30px/600-700, labels/eyebrows 11px/700 uppercase with 0.08-0.12em tracking.

## Color
- **Approach:** The accent (still keyed as `gold-400/500/600` in code - the class name is legacy, kept to avoid a repo-wide rename) went through two retunes off "Vibrant Craft"'s fire-orange: first to a deep maroon/burgundy, then - because the maroon read as too dim/muted rather than "alive" - to a warm copper/terracotta. Copper sits deliberately in the middle: warmer and more eye-catching than maroon, but nowhere near as saturated/neon as the original fire-orange gradient treatment. Still reserved for primary CTAs, revenue figures, active/"with client" status, and brand accents. Buttons/badges on the accent use white/off-white text, not black - the accent is dark enough at every step of this that black-on-accent fails contrast. **Important implementation detail:** use `text-gray-50` for text sitting on a solid/gradient accent background, not `text-white` - a pre-existing light-mode CSS rule (`index.css`, the `.text-white` band-aid) silently repaints literal `text-white` to the theme's dark text color in light mode, which was invisible while accent buttons used `text-black`, but breaks white-on-accent text the moment the accent needs white text. `text-gray-50` isn't touched by that rule. The dark-mode neutrals stay warmed toward near-black-with-a-maroon-undertone, and light-mode neutrals stay genuine beige/cream, so the palette reads as "classic barbershop" rather than tech-neutral even as the accent hue itself has moved twice.
- **Dark mode (default):**
  - Background: `#120a0c`
  - Surface: `#1c1114`
  - Surface (secondary/shell): `#271820`
  - Border: `#3a2530`
  - Text: `#f5efe8`
  - Text muted: `#a89a8c`
  - Gold/accent (copper): `#c97b3d`, Gold bright (hover/emphasis): `#e2924d`, Gold deep (pressed): `#9c5a28`
- **Light mode:**
  - Background: `#f3e9d8` (beige)
  - Surface: `#fffcf5` (ivory)
  - Surface (secondary/shell): `#e8dcc2` (deeper beige)
  - Border: `#d8c7a0` (tan)
  - Text: `#2a1f18` (warm near-black brown, not neutral gray-black)
  - Text muted: `#7a6a56`
  - Gold/accent (copper): `#8a4a1d`, Gold bright: `#a85f27`, Gold deep: `#6b3814`
- **Semantic:** dark values are this app's original Tailwind defaults (unchanged); light values shift deeper/more saturated, the standard move for text and badge tints sitting on a near-white surface instead of a near-black one.
  - Success (green): `#4ade80` (dark) / `#16a34a` (light)
  - Error (red): `#f87171` (dark) / `#dc2626` (light)
  - Warning (amber): `#fbbf24` (dark) / `#b45309` (light)
  - Info (blue): `#60a5fa` (dark) / `#2563eb` (light)
  - Implemented as `--tc-green-400/500/600`, `--tc-red-400/500/600`, `--tc-amber-400/500/600`, `--tc-blue-400/500/600` (only the shades this app actually uses are theme-aware; other shades and the one-off purple/emerald/yellow accents still use Tailwind's literal defaults - a known, accepted gap in the incremental rollout).
- **Theme switching:** implemented via `data-theme` attribute on `<html>` plus CSS custom properties - see `frontend/src/index.css`. A theme toggle belongs in the global Settings surface (see below) and should persist the user's choice (e.g. localStorage), defaulting to **light** (per explicit user request; a saved preference of either theme still always wins over the default).
- **Dark-theme page background - "glossy black":** `html[data-theme='dark'] .app-shell` (App.tsx's root div - the opaque canvas that actually paints behind every route, not `body`, which it fully covers and is therefore invisible if targeted instead - this cost one iteration, verified live via `elementFromPoint`) overrides the fill to a true neutral near-black (`#050505`, not `--tc-dark-900`'s `#120a0c`, which has a warm maroon undertone that reads as brown/hazy once any highlight sits on it) and layers two small, low-opacity (1.8-5%), tightly-sized radial-gradient highlights (top-center and bottom-right, `background-attachment: fixed`) over it, so the page canvas reads as a polished black surface rather than flat matte black or a visible warm haze. Deliberately scoped to `.app-shell`'s own fill only - not the `--tc-dark-900` token - so every component that reuses that token for its own surface (cards, inputs, badges, the clock pill, etc.) is unaffected, per explicit user request ("make the background glossy black, leave everything else as is"). Light theme is untouched. Not visible on the WelcomePage hero band specifically, since that band paints its own opaque fixed-color background on top (see the hero band note above).

## Spacing
- **Base unit:** 8px.
- **Density:** Comfortable - data-dense admin/report screens stay grid-disciplined, but receptionist/barber-facing screens (used on the move, sometimes on tablets) keep touch-friendly spacing.
- **Scale:** xs(4) sm(8) md(16) lg(24) xl(32) 2xl(48).

## Layout
- **Approach:** Hybrid - grid-disciplined for dashboards, schedules, and reports (predictable columns, strict alignment); slightly more editorial on the client-facing welcome/marketing page (asymmetric hero, looser rhythm).
- **Max content width:** 1180px for dashboard/report views.
- **Border radius:** sm 6px (buttons, inputs, badges), md 10px (stat tiles), lg 16px (cards, panels), full 999px (pills/toggles).
- **Hero eyebrow label:** a small bold uppercase, tight-tracked accent-colored line directly above the H1 (e.g. "Zimbabwe's Premier Barbershop" above "Precision Grooming & Executive Barbershop Platform") - a standard marketing-hero convention, added per explicit user request referencing a car-marketplace reference site's "ZIMBABWE'S CAR MARKETPLACE" label sitting the same way above its own headline.
- **WelcomePage hero band:** the hero (Section 1 - logo, eyebrow, headline, CTA buttons; the clock moved into the Navbar and the feature-pill row was later removed - see the Decisions Log) sits in its own full-bleed band with a fixed dark background (`#15100d`, a literal hex, not a `--tc-*` token) regardless of the site's light/dark theme toggle. Per explicit user request: in light theme, the hero previously shared the same light beige background as the navbar above it and the role-selection section below it, reading as "washed out" with no visual separation - matching the car-marketplace reference site's light-navbar/dark-hero contrast fixed this. Every color inside this band (text, icons, pill backgrounds, gradients) is hardcoded to a fixed value rather than a theme token, since it must stay legible against this permanently-dark backdrop no matter which theme is active - notably using `text-gray-50`/`text-gray-200`/`text-gray-300` (Tailwind literals not remapped by `tailwind.config.js`) rather than the app's usual `text-white`/`text-gray-400`, both of which are theme-reactive (or, for `text-white` specifically, actively repainted to the theme's dark text color in light mode - see the note above) and would render illegibly dark against this permanently-dark band once the site is switched to light theme.

## Motion
- **Approach:** CSS-3D floating cards - every panel now carries a visible shadow at rest, reading as physically lifted off the page, with a stronger lift-and-glow on interaction. This part of "Vibrant Craft" was kept through the "Quiet Precision" pivot - it reads as tactile polish, not flashiness. Per explicit original user request ("all boxes should be floating 3D designs with shadow... on all pages/dashboards").
- **Signature interaction:** `.card-3d` - permanent resting shadow (`0 14px 32px -10px rgba(0,0,0,0.4), 0 4px 10px -4px rgba(0,0,0,0.25)`), hover intensifies to `transform: translateY(-6px) rotateX(2deg)` with a deeper shadow and a copper accent border glow (`rgba(201, 123, 61, ...)`). Applied to barber status cards, stat tiles, and any clickable or informational panel - i.e. nearly every card in the app.
- **Easing:** enter `cubic-bezier(0.16, 1, 0.3, 1)` (ease-out), short/medium durations (150-350ms).
- **System-wide transitions (page/modal):** short (150-180ms), opacity-only or opacity+transform animations - GPU-accelerated, no layout cost - so the app reads as "smooth" without ever feeling slow to respond, per explicit user request to add transitions "but one that doesn't feel like the system is slow" and to prioritize loading speed. `.page-fade-in` (180ms, **opacity-only**) wraps the routed page content in `App.tsx`, keyed by `location.pathname` so every navigation re-triggers it. `.modal-backdrop-in` (150ms fade) and `.modal-panel-in` (180ms fade+scale-from-0.96, same ease-out curve as `.card-3d`) apply to every full-screen modal in the app (WelcomePage's role sign-in, BarberDashboard's block-out form, ReceptionistPortal's walk-in form and receipt) for a consistent entrance instead of a hard pop-in. All respect `prefers-reduced-motion: reduce` (animations disabled entirely). **`.page-fade-in` is opacity-only, deliberately** - it originally also animated a 4px `translateY`, but any `transform` on an element (even one whose keyframe ends at an identity/no-op value) makes that element a new CSS containing block for any `position: fixed` descendant. Since `.page-fade-in` wraps every routed page, that broke every full-screen modal's centering site-wide - `fixed inset-0` on the modal backdrop was being sized/positioned against `.page-fade-in`'s own (scrolled, full-page-height) box instead of the true viewport, so modals rendered far off-center on any page taller than one screen. Found via a user report ("when a person opens the login button the login interface should be centered") and confirmed live with `getBoundingClientRect()` before/after.
- **Hero glass panel:** the WelcomePage hero's content (logo, headline, CTAs) sits inside a frosted-glass bar (`bg-white/[0.06]`, `backdrop-blur-xl`, top/bottom border only, `shadow-2xl`; dark theme gets a brighter gradient sheen + border since the subtle version read as barely-there against near-black) rather than directly on the flat dark band, so the animated corner glows diffuse through the translucent backdrop behind the text - per explicit user request ("make the hero band glassy" / "make it glassy in dark mode too"). The bar is full-bleed (no side margin against the hero band's own edges) and feathered with a left/right `mask-image` gradient so the glass dissolves into the dark band at the edges instead of stopping at a hard rounded-corner boundary - per explicit follow-up request ("make the glassy on the hero bar fade away on the left and right edge").
- **Roles-section glass cards:** the five role-selection cards (and their icon badges) use the same frosted-glass treatment (`bg-white/[0.04]` resting, `bg-white/10` on hover, `backdrop-blur-md`, translucent border) instead of a solid `bg-dark-800` fill, extending the hero's glass language into the theme-reactive section below it - per explicit user request ("make it glassy on the roles section too").
- **Background glows (WelcomePage hero band):** four low-opacity (8-16%) blurred blobs, one anchored in each corner of the hero band, "dancing" - the two diagonal pairs (top-left+bottom-right vs top-right+bottom-left) sway in mirrored/opposite directions at the exact same time (`.hero-corner-a`/`.hero-corner-b` in `index.css`: same 8s duration, no delay offset between the two direction keyframes, so one diagonal pair is always moving opposite to the other, simultaneously - a synchronized cross-pattern, not independent random drift). Opacity still flickers per-blob with a slight stagger for texture. This replaced an earlier independent-drift version and, before that, the "Vibrant Craft" era's `animate-pulse` (a uniform pulse that read as a generic AI-landing-page tell) - per explicit user request ("dancing in the corners, opposite but simultaneous"). **Theme-specific treatment:** in light theme, all four corner blobs use the copper accent only (calmer); in dark theme, the hero background itself is pure near-black (`#050403` vs light theme's `#15100d`) and the four corners alternate between the copper accent and blue (`#3b82f6`/`#2563eb`) for a "fire" effect, per explicit user request ("in dark mode, make it black with fire animation that is blueish and orange").

## Settings (cross-cutting requirement)
Every role's dashboard (Company Admin, Receptionist, Barber, Client) should expose a Settings surface with, at minimum:
- Light/Dark theme toggle (persisted per-user)
- Profile/account basics (name, phone, branch where applicable)
- Notification preferences (SMS reminders, etc.)

This is a functional requirement layered on top of the visual system above, not a separate visual direction - Settings screens should use the same components, spacing, and typography as the rest of the app.

Additionally, a compact theme toggle (sun/moon icon) lives directly in the Navbar for logged-out visitors only - this lets a visitor change the site-wide color theme before ever signing in, rather than it only being reachable through the logged-in-only `/settings` page. It disappears once a user is logged in, since every logged-in page already has a Settings entry point with its own theme toggle (and `/settings` itself, the toggle's own destination, obviously doesn't need a second one) - per explicit user request ("dashboards with the settings button don't need that theme button for its already in the setting tab").

The Navbar's Logout button, by contrast, is hidden on the public WelcomePage (`/` and `/welcome`) even when a user is authenticated - logging out from a marketing/landing page reads as an odd, out-of-place action there. It still renders on every actual dashboard/operational route.

## Session Security (cross-cutting requirement)
Authenticated pages must not remain visible via the browser's back/forward button after logout. Two mechanisms enforce this:
- Logout performs a hard navigation (`window.location.href`), not a client-side route change, so the authenticated page is fully torn down.
- A `pageshow` listener (see `AuthContext.tsx`) forces `window.location.reload()` whenever the browser restores a page from its back/forward cache (`event.persisted`), so a stale, frozen dashboard is never left on screen - the app always re-checks auth state from scratch, and an already-logged-out visitor is bounced to the login prompt.

## Reference Implementation
The `.card-3d` hover pattern, color tokens, and typography roles above are demonstrated in a working HTML preview generated during the design consultation (dark + light toggle, typography specimen, color swatches, and a live-data Company Admin Dashboard mockup). Use it as the visual source of truth when implementing components.

## Decisions Log
| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-09-19 | Initial design system created (Industrial-Executive hybrid, dark+gold retained, Cabinet Grotesk/General Sans/Geist, CSS-3D card-lift motion) | Created by /design-consultation. Competitive research (Fresha/GlossGenius/Squire) showed category convergence on flat, generic, interchangeable UI - TrueCut differentiates via precision-engineered typography and depth. |
| 2026-09-19 | Added light theme as a required feature | User request during design consultation - Settings should let users switch themes, not just consume dark mode. |
| 2026-09-19 | Re-tuned semantic status colors (success/error/warning/info) for theme-awareness | User request - green/red/amber/blue text and badges were unthemed literal Tailwind colors, reading as washed-out on light backgrounds since they were only tuned for a dark background. |
| 2026-09-22 | Shifted to "Vibrant Craft": real logo integrated, accent warmed from brass-gold to fire-orange, display font Cabinet Grotesk -> Satoshi, `.card-3d` shadow now persistent (not hover-only) | User request, inspired by booksy.com's warmth/liveliness and the TrueCut Barber logo's fire motif - wanted more energy across every page/dashboard while keeping the dark-first precision-tool identity. |
| 2026-09-22 | Pivoted to "Quiet Precision": accent re-colored from fire-orange to maroon/burgundy, dark-mode neutrals warmed toward near-black, light-mode neutrals shifted to genuine beige/cream, display font Satoshi -> Inter, background glows de-pulsed and reduced to one hue, star/sparkle icons near pricing replaced with a standard price-tag icon, black-on-accent text switched to white for contrast | User feedback that "Vibrant Craft" looked "very vibrant, like it was created and designed by AI" rather than natural/professional; wanted a calmer, classic, standard look and specifically asked to try maroon and beige - a traditional barbershop-pole palette. |
| 2026-09-22 | Added a theme toggle to the Navbar (always visible, including logged-out) and a `pageshow`/back-forward-cache reload guard plus hard-navigation logout | User request: needed a way to change the site theme from the public welcome page (not just the logged-in Settings page), and needed the browser's Back button to never reveal a stale authenticated dashboard after logout - security concern. |
| 2026-09-22 | Retuned accent from maroon to copper/terracotta; added a hero eyebrow label above the WelcomePage H1 | User feedback that the maroon read as too dim/muted, not "alive"; wanted something eye-catching to a broad audience without going back to "vibrant." Also asked for a small label band above the headline, referencing the same car-marketplace site's eyebrow-label-above-headline pattern. |
| 2026-09-23 | Added a 6px solid black border framing the whole site (inline on `<body>` in `index.html`, not in `index.css`, due to the pipeline dropping hand-authored `body` border rules) | User request, referencing the car-marketplace reference site's black demarcation border around its page content. |
| 2026-09-23 | WelcomePage hero given a fixed permanently-dark background (independent of theme); Navbar Logout button hidden on `/` and `/welcome` | User feedback (with screenshot) that in light theme the hero blended into the navbar above and the role-selection section below with no visual separation, and that the Logout button showing up while just viewing the marketing page looked wrong. |
| 2026-09-23 | Animated the hero's background glow blobs (slow drift + flicker, staggered per-blob); dark theme's hero background changed to pure black with an added blue+orange "fire" blob treatment (light theme keeps the calmer copper-only drift) | User request for "distinct but classic" animation on the hero's blurred background, and specifically for dark mode to be "black with fire animation that is blueish and orange." |
| 2026-09-23 | Reworked the hero glow blobs into a four-corner layout with synchronized, mirrored diagonal motion (`.hero-corner-a`/`.hero-corner-b`) | User request: the previous independent-drift animation should instead be "dancing in the corners, opposite but simultaneous" - diagonal pairs now move in mirrored directions at the exact same time rather than drifting independently. |
| 2026-09-23 | Recolored the hero subhead paragraph from cool gray to warm dark beige | User feedback (with screenshot) that the subhead read as washed out/low-contrast against the hero's warm dark background. |
| 2026-09-23 | Wrapped the hero's content in a frosted-glass card (translucent white, `backdrop-blur-xl`, subtle border, rounded) instead of it sitting directly on the flat dark band | User request: "make the hero band glassy." |
| 2026-09-23 | Strengthened the hero glass card's dark-theme treatment (brighter gradient sheen + border) | User feedback that the glass effect was too subtle to read as "glassy" against dark theme's near-black hero background specifically. |
| 2026-09-23 | Extended the frosted-glass treatment to the five role-selection cards (and their icon badges) | User request: "make it glassy on the roles section too." |
| 2026-09-23 | Made the hero glass panel full-bleed (removed the side max-width/margin) with a left/right `mask-image` fade | User request: "make the glassy on the hero bar fade away on the left and right edge" - the card's previous fixed-width rounded-corner boundary is now a full-width bar that dissolves into the dark band at both edges instead. |
| 2026-09-23 | Moved the "HARARE MAIN BRANCH: [time] CAT" clock from the WelcomePage hero into the Navbar, absolutely centered on the nav bar itself (not just a flex child) | User request: "remove this and put it in the navbar and center justify" - the Navbar already had its own simpler clock (no location prefix, not truly centered); it now carries the hero's full label and is centered regardless of how wide the brand/nav-links sections are on either side. |
| 2026-09-23 | Removed the site-wide 6px black frame border | User feedback ("the tiny spacing around the whole interface is still there") clarified as "remove the black margin" - the border itself (added 2026-09-23) was the "spacing," not a layout bug; reverted. |
| 2026-09-23 | Added short (150-180ms), opacity/transform-only page and modal transitions (`.page-fade-in`, `.modal-backdrop-in`, `.modal-panel-in`), respecting `prefers-reduced-motion` | User request: "add smooth transition animations to the system, but one that doesn't feel like the system is slow cz we want to maximise loading speed" - kept every new animation short and GPU-cheap rather than reaching for longer/heavier transitions. |
| 2026-09-23 | Fixed a mobile-width Navbar overflow bug: the right-side item group (Prices, theme toggle, Dashboard, Settings, Logout) could exceed a phone viewport's width and get clipped off-screen. "Prices" now hides below `sm`, and right-side gaps tighten to `space-x-1` below `sm` | Found while checking the app in mobile view per user request - confirmed via `document.documentElement.scrollWidth` exceeding `clientWidth` on `/book` while logged in as a non-staff user (the item-count-heaviest case). |
| 2026-09-23 | Added a "glossy black" treatment to the dark-theme page background (two soft radial-gradient highlights layered on `body`'s own background-image, not the `--tc-dark-900` token) | User request: "make the background glossy black, leave everything else as is" - scoped to `body` specifically so no component's own bg-dark-900/etc surface changed. |
| 2026-09-23 | Fixed the glossy-black treatment: moved it from `body` (invisible - fully covered by App.tsx's opaque `.app-shell` root div) to `.app-shell` itself, and switched its base fill from `--tc-dark-900` (`#120a0c`, warm maroon undertone) to a true neutral `#050505` | User report (with a marked-up screenshot) that the background still wasn't reading as black on dashboard pages; confirmed live via `elementFromPoint` that `body`'s background was entirely hidden, and that any white highlight on `#120a0c` read as a brown haze rather than a black sheen. |
| 2026-09-23 | Recolored the WelcomePage role sign-in modal panel to `bg-black/95` in dark theme (was `bg-dark-800/95`, `#1c1114` - the same warm-undertone problem as the page background) | User report (marked-up screenshot) that the modal's background wasn't black in dark theme. |
| 2026-09-23 | Removed the `translateY` transform from `.page-fade-in`'s keyframe (opacity-only now) | User report that the role sign-in modal wasn't centered - root cause was `.page-fade-in`'s transform (even at its identity end-value) making the page wrapper a new fixed-positioning containing block, so every full-screen modal's `fixed inset-0` was sized against that wrapper's full-page-height box instead of the viewport. Confirmed via `getBoundingClientRect()` before/after. |
| 2026-09-23 | Changed the default theme from dark to light (a saved preference in localStorage still overrides the default either way) | User request: "make it that the webapp opens the light theme as default always." |
| 2026-09-23 | Removed the hero's "Zero Double-Booking Guarantee / 10km House Calls / EcoCash Integration" feature-pill row | User request ("this is not necessary") - the hero now flows directly from the subhead into the CTA buttons. |
| 2026-09-23 | Consolidated the per-page "Return Home" button (duplicated across 7 pages: ClientDashboard, ReceptionistPortal, BarberDashboard, CompanyAdminDashboard, PublicCatalogue, BookingWizard, SystemAdminDashboard) into the Navbar itself, replacing the old "Back" button (`navigate(-1)`) in that same top-left slot | User request: "put the 'return home' button, where the back button [is]" - one consistent Return Home affordance site-wide instead of a Navbar Back button plus a near-identical duplicate button re-declared in every page's own content. |
| 2026-09-23 | Redesigned the CompanyAdminDashboard header: the "COMPANY ADMIN & ACCOUNTANT" eyebrow moved from a filled/bordered pill to a plain classic small-caps label (icon + `tracking-[0.14em]`, no background), and the H1 grew from `text-2xl font-extrabold` to `text-3xl font-black tracking-tight` | User request: "redesign this part to make it more professional, classic and aesthetically pleasing" - the boxed-chip label read as a flat UI widget; an unboxed wide-tracked label is the more standard "executive report" convention this dashboard is going for. |
| 2026-09-23 | Applied the same eyebrow-label + heading treatment to every other dashboard header (Barber's "Barber Station" + Scissors icon, Receptionist's "Receptionist PWA" + UserCheck icon, Client's new "Client Portal" + User icon eyebrow, System Admin's "System Admin Master Control" + Shield icon, kept red instead of copper to preserve its high-privilege signaling) | User request: "do the same for all dashboards" - one consistent header convention across every operational dashboard instead of just Company Admin's. |
| 2026-09-23 | Hid the Navbar's theme toggle for logged-in users (visible only when logged out) | User request: "dashboards with the settings button don't need that theme button for its already in the setting tab" - every logged-in page already reaches a theme control via Settings, so the Navbar toggle was a redundant second control there. |
| 2026-09-23 | Made Navbar Logout red at rest (not just on hover) and gated it behind a `window.confirm()` prompt | User asked whether Logout and Return Home (which land on the same screen) were redundant, then asked for "the best way which is extremely secure." They are not redundant - Return Home preserves the session, Logout ends it - but that distinction was only visible on hover, which risked a user mistaking Return Home for a safe sign-out on a shared device. Logout is now unmistakable at rest and can't fire from a stray click. |
| 2026-09-23 | Fixed booking-status badge colors: ClientDashboard's "Served" moved from blue to green and "No Show" from purple to red; CompanyAdminDashboard's weekly report "No-Show" column moved from amber to red | User request: "'Served' should be green and 'No Show' should be red." BarberDashboard already used green/red correctly and needed no change. |
